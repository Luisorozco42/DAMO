<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentapp" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentApp" >
        <activity
            android:name="com.example.arrendador.MainArrendadorActivity"
            android:exported="false"
            android:label="@string/title_activity_main_arrendador"
            android:theme="@style/Theme.RentApp.NoActionBar" />
        <activity
            android:name=".main_arrendador"
            android:exported="false"
            android:label="@string/title_activity_main_arrendador"
            android:theme="@style/Theme.RentApp.NoActionBar" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".arrendatario.arendatario_menu"
            android:exported="true"
            android:label="@string/title_activity_arendatario_menu"
            android:theme="@style/Theme.RentApp.NoActionBar" />
        <activity
            android:name=".main_arrendatario"
            android:exported="true" />
        <activity
            android:name=".iniciar_sesion"
            android:exported="true" />
        <activity
            android:name=".registrar_usuario"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>